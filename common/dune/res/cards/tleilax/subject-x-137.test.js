const card = require('./subject-x-137.js')

describe('Subject X-137', () => {
  test('data', () => {
    expect(card.id).toBe('subject-x-137')
    expect(card.name).toBe('Subject X-137')
    expect(card.source).toBe('Immortality')
    expect(card.compatibility).toBe('All')
  })
})
