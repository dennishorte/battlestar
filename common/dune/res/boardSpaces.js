// Board space definitions — effects are TODO, need actual game data.
// Structure and metadata are placeholders until verified against the physical board.

const boardSpaces = []

module.exports = boardSpaces
